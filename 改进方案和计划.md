# AgentScope.NET 深度改进方案与计划

## 文档信息
- **分析日期**: 2026-03-15
- **Java 版本**: agentscope-java v1.0.9+
- **.NET 版本**: agentscope.net v1.1.0
- **分析人员**: AI Agent 深度研究

---

## 执行摘要 Executive Summary

经过对两个项目的深入代码级分析，发现 **agentscope-java** 在以下关键领域显著领先：

| 维度 | Java 领先度 | 关键差距 |
|------|-----------|---------|
| **架构成熟度** | ⭐⭐⭐⭐⭐ | Reactive Streaming、完整的 Hook 系统 |
| **功能完整性** | ⭐⭐⭐⭐⭐ | Skill 系统、MCP 协议、TTS、多模态 |
| **企业级特性** | ⭐⭐⭐⭐ | State 持久化、ToolGroup、SubAgent |
| **工具生态** | ⭐⭐⭐⭐ | 文件工具、代码工具、MCP 工具 |
| **开发者体验** | ⭐⭐⭐ | 更丰富的 Builder 模式、类型安全 |

**.NET 版本的优势**: Workflow 引擎（Java 无此模块）、更现代的 Pipeline API

---

## 0. 审慎可行性复核（基于当前代码实情）

> 本节是对“是否缺失、能否落地、工期是否真实”的二次校准。若后文与本节冲突，以本节为准。

### 0.1 代码基线核对结论（2026-03-15）

通过对 `src/AgentScope.Core` 与测试项目的核对，发现原计划中部分“完全缺失”判断偏保守，现校准如下：

| 能力 | 原判断 | 实际情况（校准后） | 可行性影响 |
|------|--------|-------------------|------------|
| Agent 层流式接口 | ❌ 缺失 | ⚠️ `IAgent` 仅有 `IObservable<Msg>`，无统一 `Event` 流 | 需要新增接口，但可增量兼容 |
| 模型层流式传输 | ❌ 缺失 | ✅ `IHttpTransport.StreamAsync` 已存在，多个模型已有 `GenerateStreamAsync` | 可复用，降低改造成本 |
| Hook 体系 | ❌ 严重缺失 | ⚠️ 已有 Pre/Post Reasoning/Acting；缺 Chunk/Error/Summary/TTS | 属于扩展，不是从零 |
| State 能力 | ❌ 全缺失 | ⚠️ Session/Workflow/Interruption 已有状态字典与保存机制，但缺统一 `IStateModule` 持久化协议 | 需做“统一抽象层” |
| GenerateOptions | ❌ 缺失 | ⚠️ 已有 `GenerateOptions/ToolChoice/ResponseFormat`，但缺 `ExecutionConfig` 与统一超时重试语义 | 需统一到模型调用链 |
| 文件/代码工具 | ❌ 全缺失 | ⚠️ 已有 `CodeExecutionTool`（基础版），缺命令白名单、沙箱边界、文件工具 | 安全设计优先 |
| Web 搜索工具 | - | ⚠️ 当前为 placeholder 行为 | 需尽快替换为真实实现 |
| MCP/Skill/ToolGroup/SubAgent/TTS/WebSocket | ❌ 缺失 | ❌ 核心实现确实缺失 | 需要新增模块与依赖 |

### 0.2 稳定性基线

- 当前主干测试基线良好：核心测试通过；集成测试在本次会话中因取消构建被中断，不构成代码失败信号。
- 现有架构可承载增量演进，不建议“大爆炸式重构”。

### 0.3 可行性关键判断

1. **总体可行，但原工期偏乐观**：16-20 周更接近“理想并行”估算，按当前代码耦合度与回归风险，建议调整为 **22-30 周**。
2. **首要风险不是编码量，而是跨层一致性**：`Agent -> Model -> Formatter -> Tool -> Session` 的语义统一最易造成回归。
3. **应采用“兼容优先”的双轨升级**：先加新接口与适配器，再逐步迁移旧实现，避免破坏现有 API。
4. **安全能力必须前置**：文件/命令工具、MCP 外部工具调用需先建立安全边界与审计。

### 0.4 先决条件（不满足则不进入大规模开发）

- 明确目标：优先“Java 对齐”还是“.NET idiomatic 优先”，避免反复返工。
- 冻结最小兼容面：对外公共 API 变更需版本策略（建议最少一次 Minor + 可选 Major 窗口）。
- 建立性能与回归门槛：流式延迟、内存上限、工具调用安全测试必须先有基线。

---

## 第一部分：Java 版本领先特性深度分析

### 1.1 核心架构差距

#### 1.1.1 Reactive Streaming 架构

**Java 实现** (`StreamableAgent.java`):
```java
public interface StreamableAgent {
    // 使用 Project Reactor Flux 实现真正的流式处理
    Flux<Event> stream(List<Msg> msgs, StreamOptions options);
    
    // 支持结构化输出的流式处理
    Flux<Event> stream(List<Msg> msgs, StreamOptions options, Class<?> structuredModel);
}
```

**.NET 现状**:
- ❌ 无流式 Agent 接口
- ❌ 使用 `IObservable<Msg>` 但非真正的响应式流
- ❌ 缺乏 Event 系统支持

**影响**: 无法实时显示推理过程、无法构建交互式聊天界面

---

#### 1.1.2 Accumulator 模式

**Java 实现** (`accumulator/` 包):
- `ContentAccumulator` - 内容累加器接口
- `ReasoningContext` - 推理上下文管理
- `TextAccumulator` - 文本累积
- `ThinkingAccumulator` - 思考内容累积
- `ToolCallsAccumulator` - 工具调用累积

**.NET 现状**:
- ❌ 完全缺失

**影响**: 无法实现逐步推理展示、无法暂停/恢复复杂推理过程

---

#### 1.1.3 Event 系统

**Java 实现**:
```java
public class Event {
    private final EventType type;
    private final Msg message;
    private final boolean isLast;
    private final Map<String, Object> metadata;
}
```

支持的事件类型:
- `REASONING_START/CHUNK/FINISH`
- `TOOL_CALL_START/FINISH`
- `ACTING_START/FINISH`
- `SUMMARY_START/CHUNK/FINISH`

**.NET 现状**:
- ❌ 无 Event 类型定义
- ❌ Hook 事件与 Stream 事件分离

---

### 1.2 功能模块差距

#### 1.2.1 Skill 系统 ⭐ 重要

**Java 实现** (`skill/` 包，~400 行):

```java
// Skill 注册和发现
public class SkillRegistry {
    void registerSkill(String skillId, AgentSkill skill, RegisteredSkill registered);
    void setSkillActive(String skillId, boolean active);
    AgentSkill getSkill(String skillId);
}

// Skill 仓库支持
public interface AgentSkillRepository {
    Set<RegisteredSkill> scan();
    AgentSkill load(RegisteredSkill registered);
}

// 内置仓库实现
- ClasspathSkillRepository    // 从类路径加载
- FileSystemSkillRepository   // 从文件系统加载
- JarSkillRepositoryAdapter   // 从 JAR 加载

// Skill 解析
public class MarkdownSkillParser {
    AgentSkill parse(Path skillFile);
}
```

**Skill 与 Tool 的区别**:
| 特性 | Tool | Skill |
|-----|------|-------|
| 复杂度 | 单一功能 | 复合功能 |
| 状态 | 无状态 | 可有状态 |
| 激活 | 始终可用 | 动态激活 |
| 组织 | 扁平 | 分组管理 |
| 发现 | 静态注册 | 动态扫描 |

**.NET 现状**:
- ❌ 完全缺失 Skill 系统
- 仅有基础 Tool 系统

---

#### 1.2.2 MCP (Model Context Protocol) 支持 ⭐ 重要

**Java 实现** (`tool/mcp/` 包，~600 行):

```java
// MCP 客户端包装器
public abstract class McpClientWrapper implements AutoCloseable {
    abstract Mono<Void> initialize();
    abstract Mono<List<McpSchema.Tool>> listTools();
    abstract Mono<McpSchema.CallToolResult> callTool(String toolName, Map<String, Object> args);
}

// 异步客户端
public class McpAsyncClientWrapper extends McpClientWrapper {
    private final McpAsyncClient client;
}

// 同步客户端
public class McpSyncClientWrapper extends McpClientWrapper {
    private final McpSyncClient client;
}

// 工具转换
public class McpTool implements AgentTool {
    private final McpClientWrapper client;
    private final McpSchema.Tool tool;
}

// 内容转换器
public class McpContentConverter {
    static List<ContentBlock> convert(List<McpSchema.Content> contents);
}
```

**MCP 协议价值**:
- 标准化工具调用协议
- 与 Claude Desktop、Cursor 等兼容
- 工具生态互联互通

**.NET 现状**:
- ❌ 完全缺失 MCP 支持
- 需要引入 C# MCP SDK

---

#### 1.2.3 TTS (Text-to-Speech) 支持

**Java 实现** (`model/tts/` 包，~400 行):

```java
// TTS 模型接口
public interface TTSModel {
    Mono<TTSResponse> synthesize(String text, TTSOptions options);
    String getModelName();
}

// 实时 TTS 接口
public interface RealtimeTTSModel extends TTSModel {
    Flux<TTSResponse> streamSynthesize(String text, TTSOptions options);
}

// DashScope TTS 实现
public class DashScopeTTSModel implements TTSModel {
    // 支持 qwen3-tts-flash 等模型
}

// 实时 TTS 实现
public class DashScopeRealtimeTTSModel implements RealtimeTTSModel {
    // WebSocket 实时语音合成
}

// 音频播放器
public class AudioPlayer {
    void play(byte[] audioData);
    void playAsync(byte[] audioData);
}
```

**TTS 功能**:
- 文本转语音合成
- 实时流式语音
- 多种语音选择
- 语速/音调控制

**.NET 现状**:
- ❌ 完全缺失 TTS 支持

---

#### 1.2.4 多模态工具 ⭐ 重要

**Java 实现** (`tool/multimodal/`，~900 行):

```java
// OpenAI 多模态工具
public class OpenAIMultiModalTool {
    
    @Tool(name = "openai_text_to_image")
    Mono<ToolResultBlock> openaiTextToImage(
        @ToolParam String prompt,
        @ToolParam String model,  // dall-e-3, dall-e-2
        @ToolParam Integer n,
        @ToolParam String size,   // 1024x1024, 1792x1024, 1024x1792
        @ToolParam String quality, // standard, hd
        @ToolParam String responseFormat  // url, b64_json
    );
    
    @Tool(name = "openai_image_to_text")
    Mono<ToolResultBlock> openaiImageToText(
        @ToolParam String imageUrls,
        @ToolParam String prompt,
        @ToolParam String model,  // gpt-4o, gpt-4-vision-preview
        @ToolParam Integer maxTokens
    );
    
    @Tool(name = "openai_text_to_audio")
    Mono<ToolResultBlock> openaiTextToAudio(
        @ToolParam String text,
        @ToolParam String model,  // tts-1, tts-1-hd
        @ToolParam String voice,  // alloy, echo, fable, onyx, nova, shimmer
        @ToolParam String responseFormat,  // mp3, opus, aac, flac
        @ToolParam Double speed   // 0.25 to 4.0
    );
    
    @Tool(name = "openai_audio_to_text")
    Mono<ToolResultBlock> openaiAudioToText(
        @ToolParam String audioUrl,
        @ToolParam String model,  // whisper-1
        @ToolParam String language,
        @ToolParam String prompt,
        @ToolParam String responseFormat,  // json, text, verbose_json
        @ToolParam Double temperature
    );
}

// DashScope 多模态工具
public class DashScopeMultiModalTool {
    // 类似的多模态功能，针对阿里云模型优化
}
```

**多模态能力**:
- 文生图 (DALL-E 3)
- 图生文 (GPT-4 Vision)
- 文生语音 (TTS)
- 语音转文字 (Whisper)

**.NET 现状**:
- ❌ 完全缺失多模态工具

---

#### 1.2.5 SubAgent 工具 ⭐ 重要

**Java 实现** (`tool/subagent/SubAgentTool.java`，~430 行):

```java
/**
 * 将 Agent 封装为 Tool，支持多轮对话
 */
public class SubAgentTool implements AgentTool {
    
    private final SubAgentProvider<?> agentProvider;
    private final SubAgentConfig config;
    
    // 参数
    private static final String PARAM_SESSION_ID = "session_id";
    private static final String PARAM_MESSAGE = "message";
    
    @Override
    public Mono<ToolResultBlock> callAsync(ToolCallParam param) {
        // 1. 获取或创建 Session ID
        // 2. 加载 Agent 状态（如果是继续会话）
        // 3. 执行 Agent 调用（支持流式）
        // 4. 保存 Agent 状态
        // 5. 返回结果（包含 session_id）
    }
    
    private Mono<ToolResultBlock> executeWithStreaming(
            Agent agent, Msg userMsg, String sessionId, ToolEmitter emitter) {
        return agent.stream(List.of(userMsg), streamOptions)
            .doOnNext(event -> forwardEvent(event, emitter, agent, sessionId))
            .filter(Event::isLast)
            .last()
            .map(lastEvent -> buildResult(lastEvent.getMessage(), sessionId));
    }
}
```

**SubAgent 使用场景**:
- 主 Agent 调用专业子 Agent
- 多 Agent 协作任务分解
- 会话状态保持和恢复

**.NET 现状**:
- ❌ 完全缺失 SubAgent 工具

---

#### 1.2.6 ToolGroup 系统

**Java 实现** (`tool/ToolGroup.java`，~200 行):

```java
public class ToolGroup {
    private final String name;
    private final String description;
    private boolean active;  // 动态激活状态
    private final Set<String> tools;
    
    public void setActive(boolean active);
    public boolean containsTool(String toolName);
    public void addTool(String toolName);
    public void removeTool(String toolName);
}
```

**ToolGroupManager**:
```java
public class ToolGroupManager {
    void registerGroup(ToolGroup group);
    void activateGroup(String groupName);
    void deactivateGroup(String groupName);
    Set<String> getActiveToolNames();
    List<ToolSchema> getActiveToolSchemas();
}
```

**使用场景**:
- 按功能分组工具（admin, user, readonly 等）
- 动态启用/禁用工具集
- 权限控制

**.NET 现状**:
- ❌ 完全缺失 ToolGroup 系统
- Tool 是扁平化管理

---

#### 1.2.7 State 持久化系统

**Java 实现** (`state/` 包):

```java
// State 标记接口
public interface State {}

// 支持 State 的模块
public interface StateModule {
    void saveTo(Session session, SessionKey sessionKey);
    void loadFrom(Session session, SessionKey sessionKey);
    void loadIfExists(Session session, SessionKey sessionKey);
}

// 持久化配置
public record StatePersistence(
    boolean memoryManaged,
    boolean toolkitManaged,
    boolean planNotebookManaged
) {
    public static StatePersistence all() {
        return new StatePersistence(true, true, true);
    }
}

// 具体 State 实现
public record AgentMetaState(
    String id,
    String name,
    String description,
    String sysPrompt
) implements State {}

public record ToolkitState(Set<String> activeGroups) implements State {}

public record PlanNotebookState(...) implements State {}
```

**ReActAgent 集成**:
```java
public class ReActAgent extends StructuredOutputCapableAgent implements StateModule {
    @Override
    public void saveTo(Session session, SessionKey sessionKey) {
        session.save(sessionKey, "agent_meta", 
            new AgentMetaState(getAgentId(), getName(), getDescription(), sysPrompt));
        memory.saveTo(session, sessionKey);
        session.save(sessionKey, "toolkit_activeGroups", 
            new ToolkitState(toolkit.getActiveGroups()));
        planNotebook.saveTo(session, sessionKey);
    }
}
```

**.NET 现状**:
- ❌ 无 State 接口
- ❌ 无 StateModule 支持
- Session 有 Context 和 Metadata，但非持久化 State

---

#### 1.2.8 文件和代码工具

**Java 实现**:

```java
// 文件工具
tool/file/ReadFileTool.java
tool/file/WriteFileTool.java
tool/file/FileToolUtils.java

// 代码执行工具
tool/coding/ShellCommandTool.java
tool/coding/CommandValidator.java
tool/coding/UnixCommandValidator.java
tool/coding/WindowsCommandValidator.java
```

**文件工具功能**:
- 读取文件（支持大文件分块）
- 写入文件（支持追加/覆盖）
- 文件路径验证（沙箱安全）

**代码工具功能**:
- 执行 Shell 命令
- 命令白名单/黑名单验证
- 平台特定验证（Windows/Unix）
- 超时控制

**.NET 现状**:
- ❌ 无文件工具
- ❌ 无代码执行工具
- 只有 WebSearchTool 和 CodeExecutionTool（不完成）

---

### 1.3 传输层差距

#### 1.3.1 WebSocket 传输

**Java 实现** (`transport/websocket/`，~500 行):

```java
public interface WebSocketTransport {
    Mono<WebSocketConnection> connect(WebSocketRequest request);
}

public interface WebSocketConnection {
    Flux<String> receive();
    Mono<Void> send(String message);
    Mono<Void> close();
    boolean isOpen();
}

// 实现
public class JdkWebSocketTransport implements WebSocketTransport {}
public class OkHttpWebSocketTransport implements WebSocketTransport {}
```

**使用场景**:
- 实时 TTS
- 流式模型响应
- 实时协作

**.NET 现状**:
- ❌ 无 WebSocket 支持
- 只有基础 HTTP Transport

---

### 1.4 Hook 系统差距

**Java 实现的额外 Hook 事件**:

```java
// 已实现的 Hook（.NET 有）
- PreReasoningEvent / PostReasoningEvent
- PreActingEvent / PostActingEvent

// Java 特有的 Hook（.NET 无）
- PreSummaryEvent / PostSummaryEvent      // 摘要阶段
- PreCallEvent / PostCallEvent            // 调用阶段
- SummaryChunkEvent                        // 摘要块事件
- ReasoningChunkEvent                      // 推理块事件
- ActingChunkEvent                         // 行动块事件
- ErrorEvent                               // 错误事件
- TTSHook                                  // TTS 钩子
```

---

### 1.5 模型系统差距

**Java 的 GenerateOptions**:

```java
public class GenerateOptions {
    private final ExecutionConfig executionConfig;
    private final Duration timeout;
    private final ToolChoice toolChoice;
    private final ResponseFormat responseFormat;
    private final Object structuredOutputSchema;
}
```

**Java 的 ExecutionConfig**:

```java
public class ExecutionConfig {
    private final int maxRetries;
    private final Duration retryDelay;
    private final Duration timeout;
    private final boolean exponentialBackoff;
}
```

**结构化输出**:

```java
public class StructuredOutputReminder {
    public static final StructuredOutputReminder TOOL_CHOICE = new StructuredOutputReminder(...);
    public static final StructuredOutputReminder SYSTEM_PROMPT = new StructuredOutputReminder(...);
}

public interface StructuredOutputCapableAgent {
    <T> Mono<T> generateStructuredOutput(List<Msg> msgs, Class<T> structuredClass);
}
```

**.NET 现状**:
- ❌ 无 GenerateOptions 完整实现
- ❌ 无 ExecutionConfig
- ❌ 无结构化输出支持

---

## 第二部分：详细改进计划

### 2.1 改进路线图

```
Wave 0: 基线治理 (1-2 周)
├── 统一现状清单（已实现/部分实现/缺失）
├── 修复 placeholder 与关键警告
└── 建立性能与安全基线测试

Wave 1: 流式与事件骨架 (3-4 周)
├── Event 系统（最小可用）
├── IStreamableAgent（增量接口，不破坏旧 API）
├── Agent 事件适配器（由现有模型流式输出桥接）
└── Hook 事件扩展第一批（Chunk/Error）

Wave 2: 状态与结构化输出统一 (3-4 周)
├── IState / IStateModule / StatePersistence
├── GenerateOptions 补齐 ExecutionConfig
├── 结构化输出统一入口
└── ToolGroup（与现有 ToolFactory 兼容）

Wave 3: 工具安全与 Skill 能力 (4-6 周)
├── 文件工具（路径沙箱）
├── 命令工具（白名单 + 超时 + 审计）
├── Skill Registry/Repository/Parser
└── 回归测试与安全测试增强

Wave 4: 生态互联 (5-7 周)
├── MCP 客户端抽象与工具桥接
├── SubAgentTool（会话状态持久化）
└── WebSocket 传输（先服务 MCP/TTS）

Wave 5: 多模态与体验完善 (4-7 周)
├── TTS（离线/在线两种模式）
├── 多模态工具（图像/音频）
├── 文档和示例完善
└── 性能调优与发布验收
```

**修订后总工期建议**: 22-30 周（2-3 名开发者，含测试与文档）

### 2.2 详细任务清单

#### Phase 1: 架构基础

##### 任务 1.1: Event 系统
**优先级**: P0
**工作量**: 3 天
**文件**:
- `Event/Event.cs`
- `Event/EventType.cs`

**实现要点**:
```csharp
public class Event
{
    public EventType Type { get; }
    public Msg Message { get; }
    public bool IsLast { get; }
    public Dictionary<string, object> Metadata { get; }
    
    // 便捷属性
    public bool IsReasoning => Type == EventType.ReasoningChunk || Type == EventType.ReasoningFinish;
    public bool IsToolCall => Type == EventType.ToolCallStart || Type == EventType.ToolCallFinish;
}

public enum EventType
{
    ReasoningStart, ReasoningChunk, ReasoningFinish,
    ToolCallStart, ToolCallChunk, ToolCallFinish,
    ActingStart, ActingChunk, ActingFinish,
    SummaryStart, SummaryChunk, SummaryFinish,
    Error
}
```

##### 任务 1.2: Accumulator 模式
**优先级**: P0
**工作量**: 5 天
**文件**:
- `Accumulator/ContentAccumulator.cs`
- `Accumulator/ReasoningContext.cs`
- `Accumulator/TextAccumulator.cs`
- `Accumulator/ThinkingAccumulator.cs`
- `Accumulator/ToolCallsAccumulator.cs`

**实现要点**:
```csharp
public interface IContentAccumulator
{
    void Accumulate(ContentBlock block);
    ContentBlock GetAccumulated();
    void Reset();
}

public class ReasoningContext
{
    private readonly TextAccumulator _textAccumulator;
    private readonly ThinkingAccumulator _thinkingAccumulator;
    private readonly ToolCallsAccumulator _toolCallsAccumulator;
    
    public List<Msg> ProcessChunk(ModelChunk chunk);
    public Msg BuildFinalMessage();
    
    // 累积内容访问
    public string AccumulatedText => _textAccumulator.GetText();
    public string AccumulatedThinking => _thinkingAccumulator.GetThinking();
}
```

##### 任务 1.3: State 持久化系统
**优先级**: P0
**工作量**: 4 天
**文件**:
- `State/IState.cs`
- `State/StateModule.cs`
- `State/StatePersistence.cs`
- `State/AgentMetaState.cs`
- `State/ToolkitState.cs`

**实现要点**:
```csharp
public interface IState { }

public interface IStateModule
{
    void SaveTo(ISession session, string sessionKey);
    void LoadFrom(ISession session, string sessionKey);
    void LoadIfExists(ISession session, string sessionKey);
}

public record StatePersistence(
    bool MemoryManaged = true,
    bool ToolkitManaged = true,
    bool PlanNotebookManaged = true
)
{
    public static StatePersistence All => new(true, true, true);
}

public record AgentMetaState(
    string Id,
    string Name,
    string Description,
    string SystemPrompt
) : IState;
```

##### 任务 1.4: StreamableAgent 接口
**优先级**: P0
**工作量**: 5 天
**文件**:
- `Agent/IStreamableAgent.cs`
- `Agent/StreamOptions.cs`

**实现要点**:
```csharp
public interface IStreamableAgent : IAgent
{
    // 基础流式接口
    IAsyncEnumerable<Event> StreamAsync(IEnumerable<Msg> messages, StreamOptions options);
    
    // 结构化输出流式
    IAsyncEnumerable<Event> StreamAsync<T>(IEnumerable<Msg> messages, StreamOptions options);
    
    // 便捷方法
    IAsyncEnumerable<Event> StreamAsync(Msg message);
    IAsyncEnumerable<Event> StreamAsync();
}

public class StreamOptions
{
    public bool IncludeReasoning { get; set; } = true;
    public bool IncludeToolCalls { get; set; } = true;
    public TimeSpan? Timeout { get; set; }
    public CancellationToken CancellationToken { get; set; }
}
```

#### Phase 2: 核心功能

##### 任务 2.1: Skill 系统
**优先级**: P1
**工作量**: 8 天
**文件**:
- `Skill/ISkill.cs`
- `Skill/SkillRegistry.cs`
- `Skill/RegisteredSkill.cs`
- `Skill/ISkillRepository.cs`
- `Skill/ClasspathSkillRepository.cs`
- `Skill/FileSystemSkillRepository.cs`
- `Skill/MarkdownSkillParser.cs`
- `Skill/SkillBox.cs`

**实现要点**:
```csharp
public interface ISkill
{
    string Id { get; }
    string Name { get; }
    string Description { get; }
    IReadOnlyList<ITool> Tools { get; }
    bool IsActive { get; set; }
}

public class SkillRegistry
{
    private readonly ConcurrentDictionary<string, ISkill> _skills = new();
    private readonly ConcurrentDictionary<string, RegisteredSkill> _registeredSkills = new();
    
    public void Register(string skillId, ISkill skill, RegisteredSkill metadata);
    public void SetActive(string skillId, bool active);
    public ISkill? Get(string skillId);
    public IEnumerable<ISkill> GetActiveSkills();
}

// Skill 仓库
public interface ISkillRepository
{
    IEnumerable<RegisteredSkill> Scan();
    ISkill Load(RegisteredSkill registered);
}
```

##### 任务 2.2: MCP 协议支持
**优先级**: P1
**工作量**: 10 天
**依赖**: 需要调研 C# MCP SDK
**文件**:
- `MCP/IMcpClient.cs`
- `MCP/McpClientWrapper.cs`
- `MCP/McpAsyncClient.cs`
- `MCP/McpSyncClient.cs`
- `MCP/McpTool.cs`
- `MCP/McpContentConverter.cs`
- `MCP/McpClientManager.cs`

**实现要点**:
```csharp
public interface IMcpClient : IDisposable
{
    string Name { get; }
    bool IsInitialized { get; }
    
    Task InitializeAsync(CancellationToken ct = default);
    Task<IReadOnlyList<McpTool>> ListToolsAsync(CancellationToken ct = default);
    Task<McpCallResult> CallToolAsync(string toolName, Dictionary<string, object> args, CancellationToken ct = default);
}

public class McpTool : ITool
{
    private readonly IMcpClient _client;
    private readonly McpToolSchema _schema;
    
    public string Name => _schema.Name;
    public string Description => _schema.Description;
    
    public async Task<ToolResult> ExecuteAsync(Dictionary<string, object> parameters)
    {
        var result = await _client.CallToolAsync(Name, parameters);
        return ConvertResult(result);
    }
}
```

##### 任务 2.3: ToolGroup 系统
**优先级**: P1
**工作量**: 4 天
**文件**:
- `Tool/ToolGroup.cs`
- `Tool/ToolGroupManager.cs`

**实现要点**:
```csharp
public class ToolGroup
{
    public string Name { get; }
    public string Description { get; }
    public bool IsActive { get; set; }
    private readonly HashSet<string> _tools = new();
    
    public void AddTool(string toolName);
    public void RemoveTool(string toolName);
    public bool ContainsTool(string toolName);
    public IReadOnlySet<string> GetTools();
}

public class ToolGroupManager
{
    private readonly ConcurrentDictionary<string, ToolGroup> _groups = new();
    
    public void RegisterGroup(ToolGroup group);
    public void ActivateGroup(string groupName);
    public void DeactivateGroup(string groupName);
    public IEnumerable<string> GetActiveToolNames();
}
```

##### 任务 2.4: 文件和代码工具
**优先级**: P1
**工作量**: 6 天
**文件**:
- `Tool/File/ReadFileTool.cs`
- `Tool/File/WriteFileTool.cs`
- `Tool/File/FileToolUtils.cs`
- `Tool/Coding/ShellCommandTool.cs`
- `Tool/Coding/CommandValidator.cs`
- `Tool/Coding/UnixCommandValidator.cs`
- `Tool/Coding/WindowsCommandValidator.cs`

**实现要点**:
```csharp
public class ReadFileTool : ITool
{
    public string Name => "read_file";
    public string Description => "读取文件内容";
    
    public async Task<ToolResult> ExecuteAsync(Dictionary<string, object> parameters)
    {
        var path = parameters["path"].ToString();
        var offset = parameters.GetValueOrDefault("offset", 0);
        var limit = parameters.GetValueOrDefault("limit", 1000);
        
        // 路径验证（沙箱安全）
        if (!FileToolUtils.IsPathAllowed(path))
        {
            return ToolResult.Fail("路径不在允许范围内");
        }
        
        var content = await File.ReadAllTextAsync(path);
        return ToolResult.Ok(content);
    }
}

public class ShellCommandTool : ITool
{
    private readonly ICommandValidator _validator;
    
    public async Task<ToolResult> ExecuteAsync(Dictionary<string, object> parameters)
    {
        var command = parameters["command"].ToString();
        
        // 命令验证
        if (!_validator.Validate(command))
        {
            return ToolResult.Fail("命令未通过安全检查");
        }
        
        var result = await ExecuteWithTimeout(command, TimeSpan.FromSeconds(30));
        return ToolResult.Ok(result);
    }
}
```

#### Phase 3: 高级特性

##### 任务 3.1: TTS 支持
**优先级**: P2
**工作量**: 6 天
**文件**:
- `Model/TTS/ITTSModel.cs`
- `Model/TTS/IRealtimeTTSModel.cs`
- `Model/TTS/TTSOptions.cs`
- `Model/TTS/TTSResponse.cs`
- `Model/TTS/DashScopeTTSModel.cs`
- `Model/TTS/DashScopeRealtimeTTSModel.cs`
- `Model/TTS/AudioPlayer.cs`

**实现要点**:
```csharp
public interface ITTSModel
{
    string ModelName { get; }
    Task<TTSResponse> SynthesizeAsync(string text, TTSOptions? options = null);
}

public interface IRealtimeTTSModel : ITTSModel
{
    IAsyncEnumerable<TTSResponse> StreamSynthesizeAsync(string text, TTSOptions? options = null);
}

public class TTSOptions
{
    public string Voice { get; set; } = "default";
    public double Speed { get; set; } = 1.0;
    public string ResponseFormat { get; set; } = "mp3";
}

public class AudioPlayer
{
    public void Play(byte[] audioData);
    public Task PlayAsync(byte[] audioData);
}
```

##### 任务 3.2: 多模态工具
**优先级**: P2
**工作量**: 8 天
**文件**:
- `Tool/Multimodal/OpenAIMultiModalTool.cs`
- `Tool/Multimodal/DashScopeMultiModalTool.cs`

**实现要点**:
```csharp
public class OpenAIMultiModalTool
{
    [Tool("openai_text_to_image", "使用 DALL-E 生成图片")]
    public async Task<ToolResult> TextToImage(
        [ToolParam("prompt", "图片描述")] string prompt,
        [ToolParam("model", "模型", required: false)] string? model = null,
        [ToolParam("size", "尺寸", required: false)] string? size = null)
    {
        var request = new ImageGenerationRequest
        {
            Prompt = prompt,
            Model = model ?? "dall-e-3",
            Size = size ?? "1024x1024"
        };
        
        var response = await _openAIClient.GenerateImageAsync(request);
        return ToolResult.Ok(response.ImageUrl);
    }
    
    [Tool("openai_image_to_text", "使用 GPT-4 Vision 分析图片")]
    public async Task<ToolResult> ImageToText(
        [ToolParam("image_urls", "图片 URL，逗号分隔")] string imageUrls,
        [ToolParam("prompt", "分析提示词", required: false)] string? prompt = null)
    {
        // 实现...
    }
}
```

##### 任务 3.3: SubAgent 工具
**优先级**: P2
**工作量**: 6 天
**文件**:
- `Tool/SubAgent/SubAgentTool.cs`
- `Tool/SubAgent/SubAgentProvider.cs`
- `Tool/SubAgent/SubAgentConfig.cs`

**实现要点**:
```csharp
public class SubAgentTool : ITool
{
    private readonly ISubAgentProvider _provider;
    private readonly SubAgentConfig _config;
    
    public string Name { get; }
    public string Description { get; }
    
    public async Task<ToolResult> ExecuteAsync(Dictionary<string, object> parameters)
    {
        var sessionId = parameters.GetValueOrDefault("session_id")?.ToString();
        var isNewSession = string.IsNullOrEmpty(sessionId);
        sessionId ??= Guid.NewGuid().ToString();
        
        var message = parameters["message"].ToString();
        
        // 创建 Agent 实例
        var agent = _provider.Provide();
        
        // 加载状态（如果是继续会话）
        if (!isNewSession && agent is IStateModule stateModule)
        {
            stateModule.LoadIfExists(_config.Session, sessionId);
        }
        
        // 执行调用
        var response = await agent.CallAsync(Msg.Builder().TextContent(message).Build());
        
        // 保存状态
        if (agent is IStateModule sm)
        {
            sm.SaveTo(_config.Session, sessionId);
        }
        
        return ToolResult.Ok(new { session_id = sessionId, response = response.GetTextContent() });
    }
}
```

##### 任务 3.4: WebSocket 传输
**优先级**: P2
**工作量**: 5 天
**文件**:
- `Model/Transport/WebSocket/IWebSocketTransport.cs`
- `Model/Transport/WebSocket/IWebSocketConnection.cs`
- `Model/Transport/WebSocket/ClientWebSocketTransport.cs`

**实现要点**:
```csharp
public interface IWebSocketTransport
{
    Task<IWebSocketConnection> ConnectAsync(WebSocketRequest request, CancellationToken ct = default);
}

public interface IWebSocketConnection : IDisposable
{
    IAsyncEnumerable<string> ReceiveAsync();
    Task SendAsync(string message, CancellationToken ct = default);
    Task CloseAsync();
    bool IsOpen { get; }
}

public class ClientWebSocketTransport : IWebSocketTransport
{
    public async Task<IWebSocketConnection> ConnectAsync(WebSocketRequest request, CancellationToken ct = default)
    {
        var ws = new ClientWebSocket();
        await ws.ConnectAsync(request.Uri, ct);
        return new WebSocketConnection(ws);
    }
}
```

#### Phase 4: 完善优化

##### 任务 4.1: 补充 Hook 事件
**优先级**: P2
**工作量**: 3 天
**文件**:
- `Hook/PreSummaryEvent.cs`
- `Hook/PostSummaryEvent.cs`
- `Hook/SummaryChunkEvent.cs`
- `Hook/ReasoningChunkEvent.cs`
- `Hook/ActingChunkEvent.cs`
- `Hook/TTSHook.cs`

##### 任务 4.2: GenerateOptions 和 ExecutionConfig
**优先级**: P1
**工作量**: 3 天
**文件**:
- `Model/GenerateOptions.cs`
- `Model/ExecutionConfig.cs`
- `Model/ToolChoice.cs`
- `Model/ResponseFormat.cs`

**实现要点**:
```csharp
public class GenerateOptions
{
    public ExecutionConfig? ExecutionConfig { get; set; }
    public TimeSpan? Timeout { get; set; }
    public ToolChoice? ToolChoice { get; set; }
    public ResponseFormat? ResponseFormat { get; set; }
    public object? StructuredOutputSchema { get; set; }
}

public class ExecutionConfig
{
    public int MaxRetries { get; set; } = 3;
    public TimeSpan RetryDelay { get; set; } = TimeSpan.FromSeconds(1);
    public TimeSpan Timeout { get; set; } = TimeSpan.FromSeconds(60);
    public bool ExponentialBackoff { get; set; } = true;
}
```

##### 任务 4.3: 结构化输出支持
**优先级**: P1
**工作量**: 5 天
**文件**:
- `Agent/IStructuredOutputCapableAgent.cs`
- `Model/StructuredOutputReminder.cs`

**实现要点**:
```csharp
public interface IStructuredOutputCapableAgent : IAgent
{
    Task<T> GenerateStructuredOutputAsync<T>(IEnumerable<Msg> messages);
    IAsyncEnumerable<Event> StreamStructuredOutputAsync<T>(IEnumerable<Msg> messages, StreamOptions options);
}

public class StructuredOutputReminder
{
    public static StructuredOutputReminder ToolChoice { get; } = new("tool_choice");
    public static StructuredOutputReminder SystemPrompt { get; } = new("system_prompt");
}
```

---

## 第三部分：技术实现建议

### 3.1 技术选型

| 功能 | 推荐技术 | 理由 |
|-----|---------|-----|
| Reactive Streaming | `IAsyncEnumerable<T>` | .NET 原生，性能好 |
| MCP SDK | 社区 SDK 或自研 | 调研 modelcontextprotocol/csharp-sdk |
| WebSocket | `System.Net.WebSockets` | 原生支持，无需额外依赖 |
| JSON Schema | `JsonSchema.Net` | 标准合规，功能完整 |
| TTS 音频 | `NAudio` 或 `CSCore` | 成熟的音频处理库 |

### 3.2 代码风格指南

**命名约定**:
- 接口前缀 `I`: `IAgent`, `ITool`, `IState`
- 异步方法后缀 `Async`: `CallAsync`, `ExecuteAsync`
- 配置类后缀 `Options`: `StreamOptions`, `GenerateOptions`

**文档注释**:
```csharp
/// <summary>
/// 执行推理阶段的 Hook 事件
/// </summary>
/// <param name="agentName">Agent 名称</param>
/// <param name="inputMessages">输入消息列表</param>
public class PreReasoningEvent : HookEvent
{
    // ...
}
```

### 3.3 测试策略

每个新功能需要:
1. **单元测试** - 覆盖核心逻辑
2. **集成测试** - 验证组件协作
3. **示例代码** - 演示使用方式

**目标覆盖率**: 85%+

---

## 第四部分：优先级矩阵

### 4.1 影响力 vs 工作量分析

```
高影响力 │  P0: Event系统    │  P1: MCP
        │  P0: 流式接口统一  │  P1: Skill系统
        │  P0: State统一抽象 │  P1: 文件/命令工具安全化
         │                   │
─────────┼───────────────────┼──────────────────
        │  P2: ToolGroup    │  P2: SubAgent
        │  P2: TTS          │  P2: 多模态
        │  P2: Hook补齐      │  P2: WebSocket
低影响力 │                   │
         └───────────────────┴──────────────────
              低工作量           高工作量
```

### 4.2 推荐实施顺序

**阶段 0 (Week 1-2)**: 基线治理
1. 基线清单与接口盘点
2. WebSearchTool placeholder 替换
3. 核心 warning 清理与测试基线固化

**阶段 1 (Week 3-6)**: 架构骨架
4. Event 系统 + IStreamableAgent（兼容式）
5. Hook 扩展（Chunk/Error）
6. 最小 Accumulator（Text + ToolCall）

**阶段 2 (Week 7-10)**: 统一抽象
7. IState/IStateModule + Session 持久化适配
8. GenerateOptions + ExecutionConfig 统一
9. 结构化输出主路径打通

**阶段 3 (Week 11-16)**: 工具与安全
10. 文件工具与命令工具（安全边界优先）
11. Skill 系统（Registry/Repository/Parser）
12. ToolGroup 与权限策略

**阶段 4 (Week 17-23)**: 生态互联
13. MCP 集成（先只读工具，再可写工具）
14. SubAgentTool + 状态恢复
15. WebSocket 传输落地

**阶段 5 (Week 24-30)**: 多模态与发布
16. TTS 与多模态工具
17. 文档、示例、性能压测
18. 发布验收与版本策略执行

---

## 第五部分：风险评估

### 5.1 技术风险

| 风险 | 可能性 | 影响 | 缓解措施 |
|-----|-------|-----|---------|
| MCP SDK 不成熟 | 中 | 高 | 准备备选方案（自研简化版） |
| WebSocket 稳定性 | 低 | 中 | 充分测试，提供降级到 HTTP |
| 音频处理复杂性 | 中 | 低 | 使用成熟库（NAudio） |

### 5.2 依赖风险

| 依赖 | 状态 | 备选方案 |
|-----|------|---------|
| modelcontextprotocol/csharp-sdk | 调研中 | 自研 MCP 客户端 |
| JsonSchema.Net | 可用 | System.Text.Json Schema |

---

## 第六部分：成功指标

### 6.1 功能完整性
- [ ] 100% Java 核心功能覆盖
- [ ] 所有 P0/P1 功能完成
- [ ] 测试覆盖率 85%+

### 6.2 性能指标
- [ ] 流式响应延迟 < 100ms
- [ ] 大文件处理内存 < 100MB
- [ ] 并发 Agent 支持 100+

### 6.3 开发者体验
- [ ] API 与 Java 版本一致率 90%+
- [ ] 完整的中文/英文文档
- [ ] 10+ 示例项目

---

## 附录 A: 文件映射表

| Java 文件 | .NET 对应文件 | 状态 |
|----------|--------------|-----|
| `agent/Event.java` | `Event/Event.cs` | ❌ 缺失 |
| `agent/StreamableAgent.java` | `Agent/IStreamableAgent.cs` | ❌ 缺失 |
| `accumulator/ReasoningContext.java` | `Accumulator/ReasoningContext.cs` | ❌ 缺失 |
| `model/transport/HttpTransport.java` | `Model/Transport/IHttpTransport.cs` | ✅ 已实现（含 StreamAsync） |
| `hook/*Reasoning*/*Acting*.java` | `Hook/IHook.cs` | ⚠️ 部分实现（Pre/Post 已有，Chunk/Error 缺失） |
| `skill/SkillRegistry.java` | `Skill/SkillRegistry.cs` | ❌ 缺失 |
| `tool/mcp/McpClientWrapper.java` | `MCP/McpClientWrapper.cs` | ❌ 缺失 |
| `model/tts/TTSModel.java` | `Model/TTS/ITTSModel.cs` | ❌ 缺失 |
| `tool/multimodal/*.java` | `Tool/Multimodal/*.cs` | ❌ 缺失 |
| `tool/subagent/SubAgentTool.java` | `Tool/SubAgent/SubAgentTool.cs` | ❌ 缺失 |
| `tool/ToolGroup.java` | `Tool/ToolGroup.cs` | ❌ 缺失 |
| `state/State.java` | `State/IState.cs` | ❌ 缺失 |
| `transport/websocket/*.java` | `Transport/WebSocket/*.cs` | ❌ 缺失 |
| `model/GenerateOptions.java` | `Formatter/GenerateOptions.cs` | ⚠️ 部分实现（缺 ExecutionConfig 统一） |

---

## 附录 B: 术语对照表

| 中文 | 英文 | 说明 |
|-----|-----|-----|
| 推理 | Reasoning | LLM 思考过程 |
| 行动 | Acting | 工具执行阶段 |
| 观察 | Observation | 工具执行结果 |
| 技能 | Skill | 可复用的工具组合 |
| 会话 | Session | 对话上下文存储 |
| 状态 | State | 可持久化的数据 |
| 钩子 | Hook | 扩展点机制 |
| 多模态 | Multimodal | 图文音混合处理 |

---

## 第七部分：首批 12 周可执行任务包（可直接拆 Issue）

> 目标：先完成高可行性、高复用价值的底座能力，避免过早进入高耦合高风险模块。

### 7.1 迭代节奏建议

- 采用 2 周一个 Sprint，共 6 个 Sprint（12 周）。
- 每个 Sprint 固定产出：`设计说明` + `代码` + `测试` + `示例` + `变更记录`。
- 每个 Sprint 至少保留 20% 容量用于回归与修复。

### 7.2 Sprint 任务包

#### Sprint 1（Week 1-2）：基线治理与清障

**Issue P0-01: 现状能力清单自动化校验**
- 目标：把“文档状态”与“代码状态”建立可重复校验，避免后续计划漂移。
- 范围：`src/AgentScope.Core`、`tests/AgentScope.Core.Tests`。
- 交付：
    1. 关键能力扫描脚本（符号级）
    2. 状态报告（已实现/部分实现/缺失）
    3. CI 中可执行
- 验收标准：每次 PR 可自动输出能力状态差异。
- 回滚策略：仅新增脚本和报告，不影响运行时。

**Issue P0-02: WebSearchTool 去 placeholder 化**
- 目标：替换模拟结果，提供真实可配置实现（可先接一个 provider）。
- 主要文件：`src/AgentScope.Core/Tool/WebSearchTool.cs`。
- 验收标准：
    1. 能在无 key 场景优雅降级
    2. 有集成测试覆盖成功与失败路径
    3. 错误信息可定位
- 回滚策略：保留旧行为开关（配置项）。

**Issue P0-03: 核心 Warning 清理基线**
- 目标：将核心测试项目关键 nullable warning 清零或可解释化。
- 范围：`tests/AgentScope.Core.Tests`、`tests/AgentScope.Integration.Tests`。
- 验收标准：测试通过且 warning 数下降并记录。

#### Sprint 2（Week 3-4）：Event 最小闭环

**Issue P0-04: Event/EventType 最小实现**
- 目标：提供统一事件模型，先覆盖 Reasoning/Acting/Tool/Error 主路径。
- 新增文件：`src/AgentScope.Core/Event/Event.cs`、`src/AgentScope.Core/Event/EventType.cs`。
- 验收标准：至少 10 个单测覆盖事件序列与终止语义（`IsLast`）。

**Issue P0-05: Agent 事件适配器**
- 目标：在不破坏 `IAgent` 的前提下，将现有调用结果桥接为事件流。
- 策略：新增适配层，不改旧接口签名。
- 验收标准：旧 API 全通过；新适配层可消费并输出事件。

#### Sprint 3（Week 5-6）：IStreamableAgent 与 Hook 扩展

**Issue P0-06: IStreamableAgent 兼容式引入**
- 目标：新增 `IStreamableAgent` + `StreamOptions`，保持向后兼容。
- 新增文件：`src/AgentScope.Core/Agent/IStreamableAgent.cs`、`src/AgentScope.Core/Agent/StreamOptions.cs`。
- 验收标准：
    1. 现有 `IAgent` 用法不变
    2. 新接口可输出事件序列
    3. 取消令牌生效

**Issue P1-01: Hook Chunk/Error 第一批事件**
- 目标：补齐 `ReasoningChunk`、`ActingChunk`、`Error` 事件。
- 验收标准：HookManager 支持注册与触发，覆盖异常路径测试。

#### Sprint 4（Week 7-8）：State 统一抽象

**Issue P0-07: IState/IStateModule/StatePersistence**
- 目标：统一 Session/Interruption/Plan 状态存取协议。
- 新增文件：`src/AgentScope.Core/State/*`。
- 验收标准：
    1. 一个示例 Agent 可 Save/Load/LoadIfExists
    2. 断点恢复可复现

**Issue P1-02: State 与 Session 适配层**
- 目标：避免重写 Session，实现最小侵入映射。
- 验收标准：不破坏现有 Session API 与测试。

#### Sprint 5（Week 9-10）：GenerateOptions 统一与结构化输出打底

**Issue P1-03: ExecutionConfig 统一模型调用语义**
- 目标：引入 `MaxRetries/RetryDelay/Timeout/Backoff` 的统一执行配置。
- 影响面：Model + Formatter 边界。
- 验收标准：不同 provider 至少各 1 条重试/超时测试。

**Issue P1-04: 结构化输出统一入口**
- 目标：先打通 JSON Object/Schema 两种主路径。
- 验收标准：OpenAI + Anthropic + Gemini 至少覆盖其中两家一致行为。

#### Sprint 6（Week 11-12）：工具安全底座

**Issue P1-05: ReadFileTool/WriteFileTool + 路径沙箱**
- 目标：新增文件工具并限制访问根目录。
- 新增文件：`src/AgentScope.Core/Tool/File/*`。
- 验收标准：越权路径访问被拒绝并可审计。

**Issue P1-06: ShellCommandTool + 验证器**
- 目标：提供可控命令执行（白名单、超时、输出截断）。
- 新增文件：`src/AgentScope.Core/Tool/Coding/*`。
- 验收标准：Windows/Unix 双平台策略均有单测。

### 7.3 统一验收门槛（Definition of Done）

每个 Issue 合并前必须满足：
1. 单元测试与相关集成测试通过。
2. 至少一条负向测试（异常、超时、越权、空输入）。
3. 公共 API 变更有迁移说明。
4. 文档同步更新（README 或对应模块文档）。
5. 性能与内存无明显退化（与基线对比）。

### 7.4 建议里程碑设置（GitHub Milestones）

- `M1-基线治理与事件骨架`：Sprint 1-2
- `M2-流式与状态统一`：Sprint 3-4
- `M3-配置统一与工具安全底座`：Sprint 5-6

---

## 总结

通过本改进计划的实施，agentscope.net 将达到与 agentscope-java 功能对等的状态，同时在 .NET 生态中保持 idiomatic 的代码风格。关键成功因素包括：

1. **渐进式实施** - 按优先级分阶段交付
2. **充分测试** - 每功能配套测试用例
3. **文档同步** - 中英文双语文档
4. **社区反馈** - 及时响应使用者需求
5. **兼容优先** - 新接口优先增量引入，避免破坏现有 API
6. **安全前置** - 文件/命令/MCP 调用必须通过安全闸门

预计总工作量：**22-30 周（5.5-7.5 个月）**，可支持 2-3 名开发者并行开发。
