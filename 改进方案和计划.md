# AgentScope.NET 改进方案与计划（校准版）

## 文档信息
- 校准日期：2026-03-31
- 校准范围：main 分支源码、测试、示例、状态文档
- 校准方法：目录核对、关键实现抽样、测试实跑
- 当前验证基线：dotnet test AgentScope.slnx 通过 678 项测试，0 失败，0 个 warning
- 快速验证基线：pwsh ./scripts/test-fast.ps1 通过 663 项测试，0 失败
- 外部依赖 smoke 基线：pwsh ./scripts/test-external.ps1 通过 4 项测试，0 失败
- 外部依赖完整基线：pwsh ./scripts/test-external.ps1 -Full 通过 15 项测试，0 失败

## 执行摘要

当前项目的主要问题不是“核心能力普遍缺失”，而是“能力面已经铺开，但成熟度、接入度、文档口径不一致”。

前一版计划把多项已经存在的能力按零开始排期，带来了两个明显偏差：

1. 高估了从零建设的工作量。
2. 低估了把能力接入主链路、补齐真实 provider、完善验证的复杂度。

更合理的改进方向是：

1. 先纠正文档与能力扫描口径。
2. 再把 Streaming、Hook、State、ToolFactory 这些主链路能力补成真正可用。
3. 最后补 Skill、MCP、TTS、多模态的真实实现与生态建设。

## 一、事实校准

### 1.1 已有基础实现的能力

| 能力 | 当前情况 | 判断 |
|------|----------|------|
| Event / EventType | 已有事件模型与单元测试 | 具备基础语义，不是缺失项 |
| IStreamableAgent / StreamOptions / AgentStreamAdapter | 已有接口、选项、适配层 | 已有入口，但还不是真实增量流 |
| Accumulator | 已有 Text、Thinking、ToolCalls、ReasoningContext 及测试 | 具备基础累积能力 |
| Hook | 已有 Pre/Post Reasoning、Pre/Post Acting、Chunk、Error 事件与 HookManager | 事件模型部分到位 |
| State | 已有 IState、IStateModule、StatePersistence、状态对象及测试 | 持久化协议已存在 |
| ToolGroup | 已有 ToolGroup、ToolGroupManager 与测试 | 分组模型已存在 |
| 文件与命令工具 | 已有 ReadFile、WriteFile、ShellCommand、命令校验器与测试 | 安全基础能力已在仓库中 |
| MCP | 已有 IMcpClient、McpClientWrapper、McpTool 与测试 | 抽象层已存在 |
| SubAgentTool | 已有基础会话延续与测试 | 基础能力已存在 |
| TTS | 已有 ITTSModel、IRealtimeTTSModel、TTSOptions、StubTTSModel、AudioPlayer | 仅骨架，不是完整功能 |
| WebSocket | 已有接口与 ClientWebSocketTransport | 传输底座已存在 |
| 多模态工具 | 已有 OpenAIMultiModalTool 与测试 | 仅占位骨架 |
| WebSearchTool | 已有 provider 抽象、模拟降级、测试 | 成熟度高于原文描述 |

### 1.2 当前仍属“部分完成”的能力

以下能力虽然已有文件或最小实现，但离“可集成、可发布、可对外宣称已完成”还有明显差距：

1. Streaming 仍主要停留在兼容适配层。AgentStreamAdapter 目前本质上还是在 CallAsync 完成后合成事件，而不是消费模型增量输出并持续产出 chunk。
2. Hook 事件模型已在 EnhancedReActAgent 上形成 Pre/Post、Chunk、Error、Summary 的基本闭环，但 accumulator 与更多 stream consumer 之间仍缺统一接入层。
3. State 协议存在，但核心 Agent 尚未系统实现 IStateModule，状态持久化更多停留在接口层与示例层。
4. ToolGroup 已有模型和测试，但没有接入统一工具选择流程。
5. ToolFactory 仍只暴露 4 个默认工具，大量新增工具仍处于“仓库里存在、运行时不可统一发现”的状态。
6. Skill 目前已有 Registry、基础元数据、文件扫描仓库、Markdown 解析、默认 MarkdownSkill 装载，以及最小 SkillBox 运行时装配闭环；仍缺字段校验、richer metadata、绑定规则细化和更深运行时集成。
7. MCP 目前已有抽象接口、Tool 适配、基于 stdio 的真实 client、McpManager、content converter、错误映射、真实子进程回归测试，以及接入 ReActAgent 的高层回归；仍缺多客户端命名隔离、更丰富 server 兼容性与更高层运行时入口。
8. TTS 当前只有 stub 与占位播放器，没有真实 provider，也没有与 Hook 或工作流连接。
9. 多模态工具当前所有执行分支都返回“需配置 API 后接入实现”，本质仍是占位实现。
10. WebSocket 传输已有实现、回环测试与最小示例，但尚未看到与模型、TTS 或实时会话场景的端到端接入。

### 1.3 当前文档与统计口径的问题

1. docs/capability-status.md 的统计结果来自 scripts/capability-scan.ps1，而该脚本当前只检查文件是否存在，不检查测试、接入、真实 provider 或运行时可达性。
2. 因此，Implemented 目前更接近“仓库中存在对应文件”，而不是“功能已经完成”。
3. 改进方案和 TODO 不能再把“文件存在”当成“功能完成”，也不能把“接入不足”误写成“完全缺失”。

## 二、判断与意见

### 2.1 总体判断

当前项目已经跨过“从 0 到 1 的大面积铺设阶段”，进入“从 1 到可用”的整合阶段。真正的短板不再是接口数量，而是以下四点：

1. 能力成熟度没有分层。
2. 主链路接入不足。
3. 统一入口和默认暴露面落后于实现面。
4. 文档口径落后于代码现实。

### 2.2 优先级意见

1. Streaming 应维持最高优先级，因为 Hook、Accumulator、结构化输出、交互式 UI 都依赖真实事件流。
2. ToolFactory 或新的注册式入口需要尽快扩展，否则新增工具长期处于“有代码、无入口”的尴尬状态。
3. TTS、多模态、MCP、Skill 不应再按“是否存在文件”争论，而应直接围绕“真实实现是否可运行”来排期。
4. 文档和脚本应先改口径，再改数字，否则后续计划仍会建立在错误基线上。
5. 当前 678 项测试全部通过是积极信号；快速路径 663、外部依赖 smoke 4、外部依赖 Full 15 也都已可独立运行，且外部调用改为显式 opt-in，后续可直接按分层接入 CI，降低不确定性。

## 三、修订后的优先级矩阵

| 优先级 | 主题 | 目标 |
|--------|------|------|
| P0 | 基线可信度与运行时主链路 | 扫描口径修正、warning 清理、真实 streaming、hook 闭环 |
| P1 | 运行时接入与统一入口 | State 落地、ToolGroup 接入、ToolFactory 扩展、测试分层 |
| P2 | 能力完整化 | Skill 运行时深化、MCP 兼容性/更高层接入、WebSocket 端到端接入 |
| P3 | Provider 级功能落地 | 真实 TTS、多模态 provider、音频与实时路径 |
| P4 | 生态建设 | 示例、文档站点、CI gate、对外演示能力 |

## 四、分阶段实施计划

### Phase 0：基线治理与口径修正（1-2 周）

1. 将 capability-scan 升级为四级评估：文件存在、测试覆盖、主链路接入、真实 provider。
2. 清理 warning，先把质量基线拉平。
3. 统一状态文档、TODO、计划文档中的术语和数字。
4. 审视 ToolFactory 当前暴露范围，明确“默认工具集”和“高级工具集”的边界。

### Phase 1：Streaming 与 Hook 深化（2-4 周）

1. 为模型与 formatter 打通真正的增量输出。
2. 让 AgentStreamAdapter 或新的 stream agent 输出真实 chunk 事件，而不是只在 CallAsync 后补事件。
3. 让 ReasoningContext 面向增量块工作，而不是只作为最终消息拼装器。
4. 将 Chunk、Error、Summary hook 路径扩展到统一 stream consumer，而不只停留在单一 Agent 实现。
5. 增加更多 streaming 端到端测试。

### Phase 2：State、ToolGroup 与统一入口接入（2-3 周）

1. 明确至少一个核心 Agent 正式实现 IStateModule。
2. 将 ToolGroupManager 接入实际工具筛选流程。
3. 扩展 ToolFactory，或替换为注册式入口，纳入已稳定工具。
4. 为 SubAgent、State、ToolGroup 建立回归测试和示例。

### Phase 3：Skill 与 MCP 完整化（3-5 周）

1. 在现有 MarkdownSkillParser + SkillBox 基础上补字段校验、 richer metadata 与格式约束。
2. 将 Skill 与 Agent / ToolFactory / 运行时装配进一步集成，并补更多示例与回归测试。
3. 在现有 stdio MCP client + McpManager 基础上补多客户端命名隔离、richer content 转换与错误映射细化。
4. 评估是否继续接入官方 SDK、补更多 transport / server 兼容性验证，并增加更高层运行时入口与示例。

### Phase 4：TTS 与多模态落地（2-4 周）

1. 引入至少一个真实 TTS provider。
2. 明确 AudioPlayer 的跨平台策略。
3. 将 OpenAIMultiModalTool 从占位实现改为可配置真实调用。
4. 评估是否同步推进 TTSHook 或实时 WebSocket 场景。

### Phase 5：生态建设（3-4 周）

1. 补充至少 3 个示例项目。
2. 建立独立文档站点。
3. 将 capability scan 与 smoke tests 纳入 CI gate。
4. 让文档、示例、状态页面与代码版本同步演进。

## 五、工作量重估

1. 如果目标是把现有骨架打磨为“可集成、可验证、可发布”的能力，预计需要 12-18 周，1-2 名开发者。
2. 如果目标是与 Java 版本在 provider、示例、文档、生态层面尽量对齐，预计需要 16-24 周。
3. 原计划中的 22-30 周更接近“按完全缺失从零建设”的口径，不适合作为当前分支的剩余工作量估算。

## 六、完成定义

某项能力只有同时满足以下条件，才应从“部分完成”升格为“已实现”：

1. 仓库中有稳定 API 与最小实现。
2. 有单元测试或集成测试覆盖核心路径。
3. 已接入主链路或统一入口。
4. 对依赖外部 provider 的能力，至少有一个真实实现，或有明确的非目标说明。
5. 文档口径与实际行为一致。

## 七、近期里程碑建议

| 里程碑 | 目标 | 验收标准 |
|--------|------|----------|
| M1 | 基线可信 | 扫描口径修正，warning 清零，文档统一 |
| M2 | Streaming 可用 | 真实事件流、chunk hooks、streaming 测试通过 |
| M3 | 运行时接入 | ToolFactory 与 ToolGroup、State 完成主链路接入 |
| M4 | Skill / MCP 可演示 | Skill 运行时装配闭环 + McpManager + 至少一条可复用的高层接入链路 |
| M5 | 生态可试用 | 3 个示例 + 独立文档入口 |

## 八、结论

当前最重要的不是继续增加“看起来像已完成”的接口，而是把已经存在的能力分清成熟度、接入主链路、建立可靠验证，并让文档准确反映代码现实。只有这样，后续的 Skill、MCP、TTS、多模态和生态建设才不会继续建立在失真的基线上。